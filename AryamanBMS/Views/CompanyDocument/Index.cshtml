@model IEnumerable<AryamanBMS.Models.CompanyDocumentModel>

@{
    ViewData["Title"] = "Company Documents";
}

<div class="module-header">
    <div>
        <h1 class="module-title">Company Documents</h1>
        <p class="module-subtitle">
            Manage company legal and compliance documents
        </p>
    </div>

    <div class="module-header-actions">
        <a asp-action="Create" class="btn-app btn-primary-app">
            <i class="bi bi-plus-circle"></i>
            Upload Document
        </a>
    </div>
</div>

<div>
    <div class="app-card">

        <div class="card-header">
            <div>
                <h2 class="card-title">Document Register</h2>
                <p class="card-subtitle">
                    Search and manage uploaded company documents.
                </p>
            </div>
        </div>

        <div class="card-body">
            <div class="form-grid-3">
                <div class="form-group">
                    <label class="form-label">Search</label>
                    <input type="text"
                           id="searchBox"
                           class="app-input"
                           data-table-filter-target="#documentTable"
                           placeholder="Search document, category or number" />
                </div>
            </div>

        <div class="card-body">

            <div class="table-responsive">

                <table class="app-table"
                       id="documentTable">

                    <thead >

                        <tr>

                            <th>Document</th>

                            <th>Category</th>

                            <th>Document No.</th>

                            <th>Issue Date</th>

                            <th>Expiry Date</th>

                            <th>Status</th>

                           <th class="text-center">Active</th>

                            <th>Action</th>

                        </tr>

                    </thead>

                    <tbody>

                        @if (Model.Any())
                        {
                            @foreach (var item in Model)
                            {
                                var status = "Valid";
                                var badge = "badge-success";

                                if (item.ExpiryDate.HasValue)
                                {
                                    if (item.ExpiryDate.Value < DateTime.Today)
                                    {
                                        status = "Expired";
                                        badge = "badge-danger";
                                    }
                                    else if (item.ExpiryDate.Value <= DateTime.Today.AddDays(30))
                                    {
                                        status = "Expiring";
                                        badge = "badge-warning";
                                    }
                                }

                                <tr>

                                    <td>

                                        <strong>
                                            @item.DocumentName
                                        </strong>

                                    </td>

                                    <td>

                                        @item.Category?.CategoryName

                                    </td>

                                    <td>

                                        @item.DocumentNumber

                                    </td>

                                    <td>

                                        @(item.IssueDate?.ToString("dd-MMM-yyyy"))

                                    </td>

                                    <td>

                                        @(item.ExpiryDate?.ToString("dd-MMM-yyyy"))

                                    </td>

                                    <td>

                                        <span class="@badge">

                                            @status

                                        </span>

                                    </td>

                                    <td class="text-center">
                                        @if (item.IsActive)
                                        {
                                            <span class="badge-success">Active</span>
                                        }
                                        else
                                        {
                                            <span class="badge-secondary">Archived</span>
                                        }
                                    </td>

                                        <td class="text-center">
                                            <div class="table-actions">

                                                <a asp-action="Details"
                                                   asp-route-id="@item.CompanyDocumentId"
                                                   class="btn-app btn-icon btn-primary-app"
                                                   title="Details">
                                                    <i class="bi bi-eye"></i>
                                                </a>

                                                <a asp-action="Edit"
                                                   asp-route-id="@item.CompanyDocumentId"
                                                   class="btn-app btn-icon btn-secondary-app"
                                                   title="Edit">
                                                    <i class="bi bi-pencil"></i>
                                                </a>

                                                @if (item.IsActive)
                                                {
                                                    <a asp-action="Download"
                                                       asp-route-id="@item.CompanyDocumentId"
                                                       class="btn-app btn-icon btn-success-app"
                                                       title="Download">
                                                        <i class="bi bi-download"></i>
                                                    </a>
                                                }

                                                <a asp-action="Delete"
                                                   asp-route-id="@item.CompanyDocumentId"
                                                   class="btn-app btn-icon btn-danger-app"
                                                   title="Archive / Activate">
                                                    <i class="bi bi-archive"></i>
                                                </a>

                                            </div>
                                        </td>

                                </tr>
                            }
                        }
                        else
                        {
                            <tr>

                                    <td colspan="8"
                                        class="empty-state">
                                        No company documents found.
                                    </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>

@section Scripts {
    <script src="~/js/table-filter.js" asp-append-version="true"></script>
}
