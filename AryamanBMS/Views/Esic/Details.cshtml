@model AryamanBMS.Models.EsicMonthlySnapshotModel

@{
    ViewData["Title"] = "ESIC Snapshot Details";

    string monthName =
        System.Globalization.CultureInfo.CurrentCulture
            .DateTimeFormat
            .GetMonthName(Model.Month);

    string statusBadgeClass =
        Model.Status switch
        {
            FinancialConstants.StatutoryStatus.Paid => "badge-success",
            FinancialConstants.StatutoryStatus.Filed => "badge-info",
            _ => "badge-warning"
        };
}

<div class="module-header">

    <div>
        <h1 class="module-title">ESIC Snapshot Details</h1>
        <p class="module-subtitle">
            @monthName @Model.Year | FY @Model.FinancialYear
        </p>
    </div>

    <div class="module-header-actions">
        <a asp-action="Index"
           class="btn-app btn-secondary-app">
            <i class="bi bi-arrow-left"></i>
            Back
        </a>
    </div>

</div>


<div class="app-card form-card">

    <div class="card-header">
        <div>
            <h2 class="card-title">Liability Summary</h2>
            <p class="card-subtitle">
                ESIC liability generated from salary records.
            </p>
        </div>

        <span class="@statusBadgeClass">
            @Model.Status
        </span>
    </div>

    <div class="card-body">

        <div class="form-grid-4">

            <div class="form-group">
                <label class="form-label">Employee Count</label>
                <div>@Model.EmployeeCount</div>
            </div>

            <div class="form-group">
                <label class="form-label">Employee Deduction</label>
                <div>INR @Model.EmployeeDeductionTotal.ToString("N2")</div>
            </div>

            <div class="form-group">
                <label class="form-label">Employer Contribution</label>
                <div>INR @Model.EmployerContributionTotal.ToString("N2")</div>
            </div>

            <div class="form-group">
                <label class="form-label">Total Payable</label>
                <div><strong>INR @Model.TotalPayable.ToString("N2")</strong></div>
            </div>

        </div>

        <div class="form-actions">

            @if (Model.Status == FinancialConstants.StatutoryStatus.Pending)
            {
                <form asp-action="MarkFiled"
                      asp-route-id="@Model.EsicSnapshotId"
                      method="post">
                    <button type="submit"
                            class="btn-app btn-primary-app">
                        <i class="bi bi-flag"></i>
                        Mark as Filed
                    </button>
                </form>
            }

            @if (Model.Status == FinancialConstants.StatutoryStatus.Filed)
            {
                <form asp-action="MarkPaid"
                      asp-route-id="@Model.EsicSnapshotId"
                      method="post">
                    <button type="submit"
                            class="btn-app btn-success-app">
                        <i class="bi bi-check-circle"></i>
                        Mark as Paid
                    </button>
                </form>
            }

        </div>

    </div>

</div>

<div class="app-card table-card">

    <div class="card-header">
        <div>
            <h2 class="card-title">Challan Details</h2>
            <p class="card-subtitle">
                Record ESIC challan payment information.
            </p>
        </div>
    </div>

    <div class="card-body">

        <div class="table-responsive">

            <table class="app-table">

                <thead>
                    <tr>
                        <th>Challan No.</th>
                        <th>Payment Date</th>
                        <th>Bank</th>
                        <th>Mode</th>
                        <th class="text-end">Amount Paid</th>
                        <th class="text-end">Interest</th>
                        <th class="text-end">Late Fee</th>
                        <th class="text-center">Status</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model.Challans.Any())
                    {
                        foreach (var challan in Model.Challans)
                        {
                            <tr>
                                <td>@(challan.ChallanNumber ?? "-")</td>
                                <td>@(challan.PaymentDate?.ToString("dd-MMM-yyyy") ?? "-")</td>
                                <td>@(challan.BankName ?? "-")</td>
                                <td>@(challan.PaymentMode ?? "-")</td>
                                <td class="text-end">INR @challan.AmountPaid.ToString("N2")</td>
                                <td class="text-end">INR @challan.InterestAmount.ToString("N2")</td>
                                <td class="text-end">INR @challan.LateFee.ToString("N2")</td>
                                <td class="text-center">
                                    <span class="badge-success">@challan.Status</span>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8"
                                class="empty-state">
                                No ESIC challan recorded yet.
                            </td>
                        </tr>
                    }
                </tbody>

            </table>

        </div>

    </div>

</div>

@if (Model.Status != FinancialConstants.StatutoryStatus.Paid)
{
    <div class="app-card form-card">

        <div class="card-header">
            <div>
                <h2 class="card-title">Add Challan</h2>
                <p class="card-subtitle">
                    Add payment details before marking ESIC as paid.
                </p>
            </div>
        </div>

        <div class="card-body">

            <form asp-action="AddChallan"
                  method="post">

                <input type="hidden"
                       name="EsicSnapshotId"
                       value="@Model.EsicSnapshotId" />

                <div class="form-grid-4">

                    <div class="form-group">
                        <label class="form-label">Challan Number</label>
                        <input type="text"
                               name="ChallanNumber"
                               class="app-input" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Payment Date</label>
                        <input type="date"
                               name="PaymentDate"
                               class="app-input" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Bank Name</label>
                        <input type="text"
                               name="BankName"
                               class="app-input" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Payment Mode</label>
                        <input type="text"
                               name="PaymentMode"
                               class="app-input"
                               placeholder="NEFT / Online" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Amount Paid</label>
                        <input type="number"
                               step="0.01"
                               name="AmountPaid"
                               class="app-input" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Interest</label>
                        <input type="number"
                               step="0.01"
                               name="InterestAmount"
                               class="app-input"
                               value="0" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Late Fee</label>
                        <input type="number"
                               step="0.01"
                               name="LateFee"
                               class="app-input"
                               value="0" />
                    </div>

                    <div class="form-group">
                        <label class="form-label">Remarks</label>
                        <input type="text"
                               name="Remarks"
                               class="app-input" />
                    </div>

                </div>

                <div class="form-actions">
                    <button type="submit"
                            class="btn-app btn-primary-app">
                        <i class="bi bi-plus-circle"></i>
                        Add Challan
                    </button>
                </div>

            </form>

        </div>

    </div>
}

<div class="app-card table-card">

    <div class="card-header">
        <div>
            <h2 class="card-title">Documents</h2>
            <p class="card-subtitle">
                Upload ESIC challan, return or supporting documents.
            </p>
        </div>
    </div>

    <div class="card-body">

        <div class="table-responsive">

            <table class="app-table">

                <thead>
                    <tr>
                        <th>Document Type</th>
                        <th>File Name</th>
                        <th>Remarks</th>
                        <th>Uploaded On</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model.Documents.Any())
                    {
                        foreach (var document in Model.Documents)
                        {
                            <tr>
                                <td>
                                    <span class="badge-secondary">
                                        @document.DocumentType
                                    </span>
                                </td>

                                <td>
                                    <a href="@Url.Content("~/" + document.FilePath)"
                                       target="_blank">
                                        @document.FileName
                                    </a>
                                </td>

                                <td>@(document.Remarks ?? "-")</td>

                                <td>@document.UploadedOn.ToString("dd-MMM-yyyy")</td>

                                <td class="text-center">
                                    <div class="table-actions">
                                        <form asp-action="DeleteDocument"
                                              method="post">
                                            <input type="hidden"
                                                   name="id"
                                                   value="@document.EsicDocumentId" />

                                            <input type="hidden"
                                                   name="snapshotId"
                                                   value="@Model.EsicSnapshotId" />

                                            <button type="submit"
                                                    class="btn-app btn-icon btn-danger-app"
                                                    title="Delete"
                                                    onclick="return confirm('Delete this document?');">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5"
                                class="empty-state">
                                No ESIC documents uploaded yet.
                            </td>
                        </tr>
                    }
                </tbody>

            </table>

        </div>

    </div>

</div>

<div class="app-card form-card">

    <div class="card-header">
        <div>
            <h2 class="card-title">Upload Document</h2>
            <p class="card-subtitle">
                Upload ESIC challan, return or related supporting file.
            </p>
        </div>
    </div>

    <div class="card-body">

        <form asp-action="UploadDocument"
              method="post"
              enctype="multipart/form-data">

            <input type="hidden"
                   name="snapshotId"
                   value="@Model.EsicSnapshotId" />

            <div class="form-grid-3">

                <div class="form-group">
                    <label class="form-label">Document Type</label>
                    <select name="documentType"
                            class="app-select">
                        <option value="">Select Document Type</option>
                        <option value="Challan">Challan</option>
                        <option value="Return">Return</option>
                        <option value="Other">Other</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">File</label>
                    <input type="file"
                           name="file"
                           class="app-input"
                           required />
                </div>

                <div class="form-group">
                    <label class="form-label">Remarks</label>
                    <input type="text"
                           name="remarks"
                           class="app-input" />
                </div>

            </div>

            <div class="form-actions">
                <button type="submit"
                        class="btn-app btn-primary-app">
                    <i class="bi bi-upload"></i>
                    Upload
                </button>
            </div>

        </form>

    </div>

</div>