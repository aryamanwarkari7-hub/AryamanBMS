@model List<AryamanBMS.ViewModels.AttendanceSummaryViewModel>

@{
    ViewData["Title"] = "Salary Attendance Summary";
}

<div class="module-header">

    <div>
        <h1 class="module-title">
            Payroll Pay Days Summary
        </h1>

        <p class="module-subtitle">
            Monthly payable days used for salary processing
        </p>
    </div>

    <div class="module-header-actions">
        <a asp-action="Index"
           asp-route-month="@ViewBag.Month"
           asp-route-year="@ViewBag.Year"
           class="btn-app btn-secondary-app">
            Back
        </a>
    </div>

</div>

<div class="dashboard-grid">

    <div class="stats-card">
        <div class="stats-label">Employees</div>
        <div class="stats-number">@Model.Count</div>
    </div>

    <div class="stats-card">
        <div class="stats-label">Present</div>
        <div class="stats-number">@Model.Sum(x => x.PresentCount)</div>
    </div>

    <div class="stats-card">
        <div class="stats-label">Pay Days</div>
        <div class="stats-number">@Model.Sum(x => x.PayDays)</div>
    </div>

    <div class="stats-card">
        <div class="stats-label">Absent</div>
        <div class="stats-number">@Model.Sum(x => x.AbsentCount)</div>
    </div>

    <div class="stats-card">
        <div class="stats-label">Missing Days</div>
        <div class="stats-number">@Model.Sum(x => x.MissingDays)</div>
    </div>

    <div class="stats-card">
        <div class="stats-label">Unpaid Leave</div>
        <div class="stats-number">@Model.Sum(x => x.UnpaidLeaveCount)</div>
    </div>

</div>

<div class="app-card mb-3">

    <div class="card-header">
        <h5 class="card-title">
            Summary Filter
        </h5>
    </div>

    <div class="card-body">

        <form asp-action="AttendanceSummary"
              method="get">

            <div class="filter-form-row">

                <div class="form-group">

                    <label class="form-label">
                        Month
                    </label>

                    <select name="month"
                            class="app-select">

                        @for (int month = 1; month <= 12; month++)
                        {
                            var monthName =
                            new DateTime(2000, month, 1)
                            .ToString("MMMM");

                            if (month == (int)ViewBag.Month)
                            {
                                <option value="@month" selected>
                                    @monthName
                                </option>
                            }
                            else
                            {
                                <option value="@month">
                                    @monthName
                                </option>
                            }
                        }

                    </select>

                </div>

                <div class="form-group">

                    <label class="form-label">
                        Year
                    </label>

                    <select name="year"
                            class="app-select">

                        @for (int year = DateTime.Today.Year - 2;
                                                year <= DateTime.Today.Year + 2;
                                                year++)
                        {
                            if (year == (int)ViewBag.Year)
                            {
                                <option value="@year" selected>
                                    @year
                                </option>
                            }
                            else
                            {
                                <option value="@year">
                                    @year
                                </option>
                            }
                        }

                    </select>

                </div>

                <div class="filter-form-actions">

                    <button type="submit"
                            class="btn-app btn-primary-app">
                        <i class="bi bi-search"></i>
                        Load
                    </button>

                </div>

            </div>

        </form>

    </div>

</div>

<div class="alert alert-info mb-3">
    Payroll pay days are calculated from attendance, approved leave, holidays, week offs and missing days.
</div>

<div class="app-card table-card">

    <div class="card-header">
        <h5 class="card-title">
            Pay Days Summary - @ViewBag.Month/@ViewBag.Year
        </h5>
    </div>

    <div class="card-body">

        @if (!Model.Any())
        {
            <div class="empty-state">
                No payroll attendance summary found.
            </div>
        }
        else
        {
            <div class="table-responsive">

                <table class="app-table">

                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Employee</th>
                            <th>Present</th>
                            <th>On Duty</th>
                            <th>Paid Leave</th>
                            <th>Unpaid Leave</th>
                            <th>Holiday</th>
                            <th>Week Off</th>
                            <th>Marked Absent</th>
                            <th>Missing Days</th>
                            <th>Total Absent</th>
                            <th>Total Days</th>
                            <th>Pay Days</th>
                            <th>Attendance %</th>
                        </tr>
                    </thead>

                    <tbody>

                        @foreach (var item in Model)
                        {
                            <tr>

                                <td>@item.EmployeeCode</td>

                                <td>
                                    <strong>
                                        @item.EmployeeName
                                    </strong>
                                </td>

                                <td>
                                    <span class="badge-success">
                                        @item.PresentCount
                                    </span>
                                </td>

                                <td>
                                    <span class="badge-info">
                                        @item.OnDutyCount
                                    </span>
                                </td>

                                <td>
                                    <span class="badge-warning">
                                        @item.PaidLeaveCount
                                    </span>
                                </td>

                                <td>
                                    <span class="badge-danger">
                                        @item.UnpaidLeaveCount
                                    </span>
                                </td>

                                <td>
                                    <span class="badge-primary">
                                        @item.HolidayCount
                                    </span>
                                </td>

                                <td>
                                    <span class="badge-secondary">
                                        @item.WeekOffCount
                                    </span>
                                </td>

                                <td>@item.MarkedAbsentCount</td>

                                <td>@item.MissingDays</td>

                                <td>
                                    <strong>
                                        @item.AbsentCount
                                    </strong>
                                </td>

                                <td>
                                    <strong>
                                        @item.TotalDays
                                    </strong>
                                </td>

                                <td>
                                    <strong>
                                        @item.PayDays
                                    </strong>
                                </td>

                                <td>
                                    <strong>
                                        @item.AttendancePercentage.ToString("N2")%
                                    </strong>
                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>
        }

    </div>

</div>