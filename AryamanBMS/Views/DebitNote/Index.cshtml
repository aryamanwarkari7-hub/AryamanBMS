@model IEnumerable<AryamanBMS.Models.DebitNoteModel>

@{
    ViewData["Title"] = "Debit Notes";
}

<div class="module-header">
    <div>
        <h1 class="module-title">Debit Notes</h1>
        <p class="module-subtitle">Additional billing, underbilling and tax undercharge adjustments</p>
    </div>

    <a asp-action="Create" class="btn-app btn-primary-app">
        <i class="bi bi-plus-circle"></i>
        New Debit Note
    </a>
</div>

<div class="app-card">
    <div class="card-body">

        <form method="get" class="w-100">
            <div class="filter-form-row">

                <div class="form-group">
                    <label class="form-label">Search</label>
                    <input type="text"
                           name="search"
                           value="@ViewBag.Search"
                           class="app-input"
                           placeholder="Debit note, invoice, client or reason"
                           autocomplete="off" />
                </div>

                <div class="form-group">
                    <label class="form-label">Sort By</label>
                    <select name="sortBy" class="app-select">
                        <option value="CreatedOn" selected="@(ViewBag.SortBy == "CreatedOn")">Created On</option>
                        <option value="DebitNoteNo" selected="@(ViewBag.SortBy == "DebitNoteNo")">Debit Note No</option>
                        <option value="Invoice" selected="@(ViewBag.SortBy == "Invoice")">Invoice</option>
                        <option value="Client" selected="@(ViewBag.SortBy == "Client")">Client</option>
                        <option value="TotalDebit" selected="@(ViewBag.SortBy == "TotalDebit")">Total Debit</option>
                        <option value="ApprovedOn" selected="@(ViewBag.SortBy == "ApprovedOn")">Approved On</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Order</label>
                    <select name="sortOrder" class="app-select">
                        <option value="desc" selected="@(ViewBag.SortOrder == "desc")">Descending</option>
                        <option value="asc" selected="@(ViewBag.SortOrder == "asc")">Ascending</option>
                    </select>
                </div>

                <div class="filter-form-actions">
                    <button type="submit" class="btn-app btn-primary-app">
                        <i class="bi bi-search"></i>
                        Search
                    </button>

                    <a asp-action="Index" class="btn-app btn-secondary-app">
                        <i class="bi bi-arrow-clockwise"></i>
                        Reset
                    </a>
                </div>

            </div>
        </form>

    </div>
</div>

<div class="app-card table-card">
    <div class="card-header">
        <h5 class="card-title">Debit Notes</h5>
    </div>

    <div class="card-body">
        <div class="table-responsive">
            <table class="app-table">
                <thead>
                    <tr>
                        <th>Debit Note No</th>
                        <th>Invoice</th>
                        <th>Client</th>
                        <th>Reason</th>
                        <th>Taxable</th>
                        <th>CGST</th>
                        <th>SGST</th>
                        <th>IGST</th>
                        <th>Total Debit</th>
                        <th>Approved On</th>
                    </tr>
                </thead>

                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="10" class="empty-state">
                                No debit notes found.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.DebitNoteNo</td>

                                <td>
                                    <a asp-controller="Invoice"
                                       asp-action="Details"
                                       asp-route-id="@item.OriginalInvoiceId">
                                        @item.OriginalInvoice?.InvoiceNo
                                    </a>
                                </td>

                                <td>@item.OriginalInvoice?.Client?.ClientName</td>
                                <td>@item.Reason</td>
                                <td>@item.AdditionalTaxableValue.ToString("N2")</td>
                                <td>@item.CGST.ToString("N2")</td>
                                <td>@item.SGST.ToString("N2")</td>
                                <td>@item.IGST.ToString("N2")</td>
                                <td>@item.TotalDebit.ToString("N2")</td>
                                <td>@(item.ApprovedOn?.ToString("dd-MMM-yyyy") ?? "-")</td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>